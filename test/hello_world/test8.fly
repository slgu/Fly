func say_to(a, b) {
    return a + b;
}

func adapt(f, a, b) {
    return f(a, b);
}

func main() {
    res = adapt(say_to, "Hello", " World");

    print(res);

    a = say_to("Hello");
    res = a(" Steve");

    print(res);

    return 0;
}
