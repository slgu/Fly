class dog {
    name:String;
    age:Int;
    owner:String;

    func bark(to) {
        msg = "Hi " + to + " my name is" + name;
        print(msg);
    }

    func play() {
        age = 10;
        ans = age + 100;
        print(ans);
    }
}

class cat {
    name:String;
    age:Int;
    owner:String;
    another_dog:dog;
    func meow(to) {
        msg = "Hi " + to + " my name is " + name;
        print(msg);
    }

    func play() {
        ans = age + 100;
        print(ans);
    }
}

func main() {
    d = 2;
    d = 3;
    a = @dog;
    a.age = 12;
    a.name = "gsl";
    a.bark("slgu");
    a.play();
    c = @cat;
    c.name = "das";
    c.meow("sad");
    return 0;
}
