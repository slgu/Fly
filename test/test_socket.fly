func handle_req(con) {
    while(true) {
        msg = con.recv();
        print(msg);
    };
}

func main() {
    a = @server;
    a.listen(5566);
    while(true) {
        con = a.accept();
        fly handle_req(con);
    };
    return 0;
}
