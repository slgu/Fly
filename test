func abc2(a, b) {
    a = 3;
    b = 4;
    return a;
    if (a) {

    };
    while (a) {

    };
    for (a : b) {

    };
    for (a=3;a<4;a=a+1) {

    };
    c = set(1,2,3,4);
    d = map("sa":1,"asd":a);
    test((x,y -> x + y), d); 

    msg_q = chan();
    msg_q <- a;
    a <- msg_q;

    fly abc(a,b);
    fly test((x,y -> x + y), d);

    register mysig sendback(server);
    s = dispatch func1(a,b) "192.168.0.10" "8888";
}

func abc(a, b) {

}
