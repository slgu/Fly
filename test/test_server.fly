func handle_req(con) {
    while(con.is_alive()) {
        msg = con.recv();
        print(msg);
    };
}

func main() {
    a = @server;
    a.listen(5567);
    while(true) {
        con = a.accept();
        fly handle_req(con);
    };
    return 0;
}
