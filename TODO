3.implement string build in (slgu)
4.fly objcall (brian)
5.need to fix overloading by name problem() (slgu) may not do it
6.s = dispatch f(a, b) ip port (slgu)
7.easy optimization, const and if(true) if(false) optimization. (slgu)
8.debug for lambda class


build-in functions:
format:
    funcname, type_list -> "function code"

    type change:
        float()
        int()
        str()
    system:
        sleep()
        exit()
CODEGEN:
cp1.We are implicit typing like C++ template.
    Unlike ocaml type variable
cp2.when set None variable, we need class modification
    like A::null.
    (Rule: type needs to be decided when first access to it*)
cp3.
    (Small optimization)


a = @server;
a.listen()
while True:
    con = a.accept
    con.send
    close
    receive
b = @client
b.connect()
b.send
close
receivce
