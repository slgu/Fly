SAST:
1.socket code distribute key word
2. global variable added (to be done) (maybe easy)
3. set map list comprehen (to be done) (Third important)
4. implement the clojure

CODEGEN:

cp1.We are implicit typing like C++ template.
    Unlike ocaml type variable
cp2.when set None variable, we need class modification
    like A::null.
    (Rule: type needs to be decided when first access to it*)
cp3.
    (Small optimization)
